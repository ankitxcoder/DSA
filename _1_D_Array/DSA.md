# Time Complexity

big-O -bad
Theta -average
Omega -best

## Input size ( CPU Operation )

### O(1) < O( log n ) < O(n) < O(n^2) < O(n^3) < O(2^x) < O(n!)

## Time Complexity Directky proportional to Space complexity

## Time Com. Inversily Proportional to space then --> priority Chosen to Time complexity for doing coding

# Recurrence Relations

1. Substitution method
2. recursion Method
3. Master Theorem

---

---

## ===============================================================================================================

# Array

---

- 2
- size of array = m ( index)
  then Number n = m+1 ( include missing no that will also come in array but currently it missing )
- Sum of natural number (n(n+1))/2 (n=m+1) or ( (n+1)(n+2))/2
- Sum of array elements ( present No )
- missing Number == sum of natural no. -- sum of array elements

### Problem is == OverFlow ( array Size is very big )

---

# Finding address ... index elments { very big aray case }
- array of n index elements = index adress of lower bound elements + (n index - lower index) * byte need to store one element

